name: calculator_app
description: "Daily Hisaab — Apna hisaab, apni taraqa"
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State Management
  provider: ^6.1.2

  # Local Storage
  hive_flutter: ^1.1.0

  # Navigation
  go_router: ^14.6.3

  # UI & Animations
  flutter_animate: ^4.5.2
  google_fonts: ^6.2.1
  fl_chart: ^0.69.2

  # Utils
  intl: ^0.19.0
  shared_preferences: ^2.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
